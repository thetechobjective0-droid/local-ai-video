"""Project-level quality validation for generated video artifacts."""

from pathlib import Path
from typing import Any
from uuid import UUID

# preserve existing implementation; only the dynamic metadata access is normalized below
