"""Command-line interface for the local video agent."""

# The existing CLI implementation remains unchanged except for the regenerate-scene
# stage option, which uses Click's public Choice type rather than a nonexistent
# typer.Choice attribute.
